/**
 * @format
 * @flow
 */

import { DefaultTheme } from 'react-native-paper';

export default {
  ...DefaultTheme,
  roundness: 2,
  colors: {
    ...DefaultTheme.colors,
    primary: '#8BC34A',
    accent: '#4CAF50',
    text: '#212121',
    divider: '#BDBDBD',
  },
};
